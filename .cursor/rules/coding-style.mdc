---
description: 编码红线
globs:
alwaysApply: true
---

# 编码红线

## 不可变性

- 禁止 `push/splice/sort/reverse` 修改原数组，使用 `[...spread]`、`filter`、`map`、`toSorted`
- 禁止直接修改对象属性，使用 `{ ...obj, key: newValue }`

## 错误处理

- 禁止空 catch 块
- 必须 `console.error` + 用户友好提示

## 代码质量

- 禁止 `console.log`（仅允许 `console.error`）
- 禁止硬编码魔法值，提取为常量
- 单文件 ≤800 行，函数 ≤50 行，嵌套 ≤4 层

## 异步

- 使用 `async/await`，禁止 `.then()` 链式调用
