---
description: 命名规范
globs:
alwaysApply: true
---

# 命名规范

| 类型         | 规范                   | 示例              |
| ------------ | ---------------------- | ----------------- |
| 组件文件夹   | kebab-case             | `user-profile/`   |
| 工具文件     | camelCase              | `formatDate.ts`   |
| Vue 组件     | PascalCase             | `LoginForm.vue`   |
| Store / Hook | use 前缀               | `useUserStore`    |
| 常量         | UPPER_SNAKE_CASE       | `MAX_RETRY_COUNT` |
| 类型/枚举    | PascalCase             | `UserRole`        |
| 布尔变量     | is/has/can/should 前缀 | `isLoading`       |

## 禁止

- 禁止拼音命名
- 禁止无意义名称：`data`、`temp`、`flag`、`info`、`obj`
- 禁止单字母变量（循环索引 i/j/k 除外）
